package selenium_webdriver;
//Need to import Libraty of Selenium to open the long comment below

/*
import com.thoughtworks.selenium.*;
import org.junit.After;
import org.junit.Before;
import org.junit.Test;
import static org.junit.Assert.*;

public class abc4_RemoteControl_Junit4 {
	@SuppressWarnings("deprecation")
	private Selenium selenium;

	@SuppressWarnings("deprecation")
	@Before
	public void setUp() throws Exception {
		selenium = new DefaultSelenium("localhost", 4444, "*chrome", "http://connexion-live.audiencemedia.com/");
		selenium.start();
	}

	@SuppressWarnings("deprecation")
	@Test
	public void testAbc4_RemoteControl_Junit4() throws Exception {
		selenium.open("/admin");
		selenium.click("link=Newsletters");
		selenium.waitForPageToLoad("30000");
		selenium.click("link=Linh test 1");
		selenium.waitForPageToLoad("30000");
		selenium.click("//div[@id='newsletter-section-111']/div[2]/a/button");
		assertEquals("Newsletter saved", selenium.getAlert());
	}

	@SuppressWarnings("deprecation")
	@After
	public void tearDown() throws Exception {
		selenium.stop();
	}
}
*/
