package selenium_webdriver;
//Need to import Libraty of Selenium to open the long comment below

/*
import com.thoughtworks.selenium.*;
import com.thoughtworks.selenium.webdriven.WebDriverBackedSelenium;
import org.openqa.selenium.firefox.FirefoxDriver;
import org.openqa.selenium.WebDriver;
import org.junit.After;
import org.junit.Before;
import org.junit.Test;

import static org.junit.Assert.*;

public class abc4_Webdriver_Backed {
	@SuppressWarnings("deprecation")
	private Selenium selenium;

	@Before
	public void setUp() throws Exception {
		WebDriver driver = new FirefoxDriver();
		String baseUrl = "http://connexion-live.audiencemedia.com/";
		selenium = new WebDriverBackedSelenium(driver, baseUrl);
	}

	@SuppressWarnings("deprecation")
	@Test
	public void testAbc4_Webdriver_Backed() throws Exception {
		selenium.open("/admin");
		selenium.click("link=Newsletters");
		selenium.waitForPageToLoad("30000");
		selenium.click("link=Linh test 1");
		selenium.waitForPageToLoad("30000");
		selenium.click("//div[@id='newsletter-section-111']/div[2]/a/button");
		assertEquals("Newsletter saved", selenium.getAlert());
	}

	@SuppressWarnings("deprecation")
	@After
	public void tearDown() throws Exception {
		selenium.stop();
	}
}
*/
